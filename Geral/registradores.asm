# Registradores

	# $zero = constante zero
	# $v0, $v1 = retornam resultados de funções
	# de $a0 a $a3 = argumentos de funções
	# $ra = return adress = endereço de retorno de função  
	# de $t1 a $t9 = registradores temporários, que podem ser modificados por funções
	# de $s1 a $s8 = similares aos $t1-$t9, mas salvam valores